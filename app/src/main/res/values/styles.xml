<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowAnimationStyle">@style/myActivityOutEnterAnima</item>
    </style>

    <style name="myActivityOutEnterAnima" parent="@android:style/Animation.Activity">
        <!-- 定义activity的进出场动画  A-->B  B->A   -->
        <!--B进-->
        <item name="android:activityOpenEnterAnimation">@anim/enter_from_right</item>
        <!--A出-->
        <item name="android:activityOpenExitAnimation">@anim/out_to_left</item>
        <!--A进-->
        <item name="android:activityCloseEnterAnimation">@anim/enter_from_left</item>
        <!--B出-->
        <item name="android:activityCloseExitAnimation">@anim/out_to_right</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--自定义app主题-->
    <style name="MyTheme" parent="AppTheme">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="BlueTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/blue</item>
        <item name="colorAccent">@color/blue</item>
    </style>

    <style name="RedTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryDark">@color/red</item>
        <item name="colorAccent">@color/red</item>
    </style>

    <style name="PinkTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/pink</item>
        <item name="colorPrimaryDark">@color/pink</item>
        <item name="colorAccent">@color/pink</item>
    </style>

    <style name="PurpleTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryDark">@color/purple</item>
        <item name="colorAccent">@color/purple</item>
    </style>

    <style name="GreenTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryDark">@color/green</item>
        <item name="colorAccent">@color/green</item>

    </style>

    <style name="NiagaraTheme" parent="AppTheme">
        <item name="colorPrimary">@color/niagara</item>
        <item name="colorPrimaryDark">@color/niagara</item>
        <item name="colorAccent">@color/niagara</item>
    </style>

    <style name="ToolBarTitleTextAppearance">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">16sp</item>

    </style>

    <style name="PicTheme" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <!--<item name="android:windowAnimationStyle">@style/myDialogFragmentAnim</item>-->
    </style>

    <style name="myDialogFragmentAnim" parent="@android:style/Animation.Activity">
        <item name="android:windowEnterAnimation">@anim/enter_from_bottom</item>
        <item name="android:windowExitAnimation">@anim/out_to_left</item>
    </style>

    <style name="SearchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowAnimationStyle">@style/MySearchActivityAnim</item>
    </style>
    <style name="MySearchActivityAnim" parent="@android:style/Animation.Activity">
        <item name="android:windowEnterAnimation">@anim/enter_from_bottom</item>
    </style>
</resources>
